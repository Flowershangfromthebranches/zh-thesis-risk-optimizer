"""Universal strategy — fallback for unclassified disciplines."""

from .base import UniversalStrategy

# UniversalStrategy is defined in base.py and re-exported here for convenience.
# It is the default fallback when no specific discipline is detected.
