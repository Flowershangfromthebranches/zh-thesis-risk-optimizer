# Example 02: SIMILARITY_ONLY

## User Intent

只处理查重相似风险，重点保护引用。

## Before

卷积神经网络是一种前馈神经网络，通常包含卷积层、池化层和全连接层，能够自动提取图像特征，因此被广泛应用于图像识别任务中[8]。

## Diagnosis

| Risk | Reason |
| --- | --- |
| 高重复定义 | 句子接近教材式 CNN 定义 |
| 引用敏感 | 概念来源依赖文献[8]，必须保留 |

## After

本文采用卷积神经网络时，关注的重点不是重新定义其结构，而是利用卷积层对局部图像纹理的提取能力。文献[8]中关于卷积层、池化层和全连接层的基本说明仍是本文模型选择的理论依据，后续实验主要比较该结构在不同样本规模下的识别表现。

## Citation Integrity

- 文献[8]保留。
- 被引用的基础定义没有被写成作者原创观点。
- 未新增模型效果或实验结果。
